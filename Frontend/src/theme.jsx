import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ffd633',
      blue: '#040430',
      red: '#570808',
    },
    secondary: {
      main: '#390099', 
    },
  },
});

export default theme;